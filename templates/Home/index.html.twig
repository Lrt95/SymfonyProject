{# templates/Home/index.html.twig #}

{% extends "Home/layout.html.twig" %}


{% block title %}{{ parent() }} - TOTO{% endblock %}

{% block body %}
        {% for article in articles %}
                <div> {{ article.titre }} N° {{ loop.index }} </div>
                <div> {{ article.contenu1 }} </div>
                <img src="{{ article.image }}"/>
                <div> {{ article.etoiles }}</div>
                {% if (article.etoiles >= 4)%}
                        <div>Le meilleure</div>
                {% elseif (article.etoiles >=ge 2 and article.etoiles <=3)%}
                        <div>Pas trop mal</div>
                {% else %}
                        <div>A oublier</div>
                {% endif %}

                <hr/>
        {% endfor %}
{% endblock %}